require "spec_helper.rb"

describe Asset do
  it "should assign or reuse an asset_uri with #uri="
  it "should round-trip #uri= to #uri"
  it "should return a normalized uri from #uri"
  #maybe? it "should delegate to proper Asset class methods to process a URI"

end
